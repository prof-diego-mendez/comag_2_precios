services:
  - type: web
    name: merge-precios
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app
    envVars:
      - key: PORT
        value: 10000
      - key: FLASK_DEBUG
        value: false
    pythonVersion: 3.12
